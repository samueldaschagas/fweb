 // //Funcionalidade de mudar posição de elementos de uma lista (Posteriormente pode ser usado para mudar ordem)
    // $( "#sortable" ).sortable({
    //   placeholder: "ui-state-highlight"
    // });
    // $( "#sortable" ).disableSelection();